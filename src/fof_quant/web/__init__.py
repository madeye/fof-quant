"""Read-only web dashboard for browsing fof-quant runs.

The package is intentionally optional — install with `uv sync --extra web` to
get fastapi/uvicorn. Importing submodules without those deps will fail.
"""
